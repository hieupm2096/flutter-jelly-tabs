name: jelly_tabs
description: A jelly-like animated tab bar Flutter package — headless, dependency-free, 1:1 port of react-native-jelly-tabs.
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.5
  very_good_analysis: ^10.3.0
